---
phase: 05-settings-provider-display-preferences
plan: 03
type: tdd
wave: 2
depends_on: [05-02]
files_modified:
  - opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift
  - opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift
autonomous: true
requirements: [SETTINGS-04]
must_haves:
  truths:
    - "D-02: DeepSeek and MiniMax resolve and fetch independently, so one configured provider can refresh when the other is absent."
    - "Repeated and rapid display-preference changes do not alter provider API polling, cache values, ledger rows, chart inputs, chart series, or Refresh behavior."
    - "OpenAI quota fetching and cached fallback, existing polling cadence, cache merge, ledger recording, and chart-history handoff retain their existing semantics regardless of card visibility."
  artifacts:
    - path: opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift
      provides: "Injectable per-provider credential resolution in refreshAll without display preference input"
    - path: opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift
      provides: "DeepSeek-only/MiniMax-only request and display-isolation regression coverage"
  key_links:
    - from: DataFetcher.refreshAll
      to: ProviderCredentialResolver
      via: "one independently injected resolver read per provider"
      pattern: "credentialResolver"
    - from: DataFetcher.refreshAll
      to: WidgetCache and existing ledger/chart callers
      via: "unchanged cache construction with no visibility parameter"
      pattern: "WidgetCache"
---

<objective>
Reconnect refresh collection to the Keychain-first resolver while proving card visibility stays rendering-only.

Purpose: SETTINGS-04 requires the DMG credential feature without changing the established polling, cache, ledger, or chart data flow.
Output: Independent provider refresh orchestration and focused regression tests.
</objective>

<execution_context>
@/Users/aman/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aman/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-settings-provider-display-preferences/05-SPEC.md
@.planning/phases/05-settings-provider-display-preferences/05-02-SUMMARY.md
<interfaces>
From plan 05-02: `ProviderCredentialResolver` resolves one `ProviderID` from Keychain before a matching legacy fallback; tests can inject a fake store/resolver.

From `Sources/OpencodeWidgetApp/DataFetcher.swift`: `refreshAll` currently launches OpenAI quota fetch separately, reads usage/cache, merges quota fallback, and builds `WidgetCache`; retain all these operations and their signatures except the credential seam.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify independent refresh and display-isolation regression behavior</name>
  <read_first>opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift; opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift; .planning/phases/05-settings-provider-display-preferences/05-SPEC.md</read_first>
  <files>opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift</files>
  <behavior>
    - A DeepSeek-only resolver triggers only DeepSeek balance retrieval; a MiniMax-only resolver triggers its credit and usage retrieval.
    - Missing one provider does not suppress the other provider's request, OpenAI task, usage rows, or cached fallback behavior.
    - Running the same refresh fixtures while every visibility value changes yields the same requests, cache values, ledger-ready fields, and chart inputs because display preferences are absent from refresh APIs.
  </behavior>
  <action>Add failing `DataFetcherTests` using the established `MockURLProtocol`, temporary database/cache fixtures, and injected resolver closure/protocol. Capture selected endpoint requests and compare complete cache fields relevant to cache, ledger, and charts across independently configured provider states. Assert no test needs the host Keychain or a real display preference object. Commit the intentional red tests as `test(05-03): specify independent provider refresh`.</action>
  <verify><automated>rm -rf /tmp/opencode-widget-phase05-03 && cd opencode-widget && swift test --scratch-path /tmp/opencode-widget-phase05-03 --filter DataFetcherTests</automated></verify>
  <done>The focused refresh tests fail only because refreshAll still uses the all-or-nothing legacy credentials contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve and fetch each provider without changing data semantics</name>
  <read_first>opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift; opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift; opencode-widget/Sources/OpencodeWidgetShared/ProviderCredentialResolver.swift</read_first>
  <files>opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift, opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift</files>
  <behavior>
    - refreshAll accepts a testable resolver seam with the production Keychain-first resolver as default.
    - Each provider's network calls are guarded only by its own resolved credential.
    - Visibility is neither accepted by nor consulted in refreshAll, and all existing OpenAI/cache/ledger/chart behavior remains intact.
  </behavior>
  <action>Replace the combined `AuthReader.readCredentials` gate in `refreshAll` with independently resolved DeepSeek and MiniMax values. Launch only the selected provider calls whose key exists, retain MiniMax's credit/usage fallback and all OpenAI stale-while-revalidate/cache merge behavior, and retain usage, `WidgetCache`, ledger, and chart-history semantics. Do not add a visibility argument, preference import, or visibility branch to `DataFetcher` or its callers. Make the red tests green and commit `feat(05-03): keep collection independent of card visibility`.</action>
  <verify><automated>rm -rf /tmp/opencode-widget-phase05-03 && cd opencode-widget && swift test --scratch-path /tmp/opencode-widget-phase05-03 --filter DataFetcherTests</automated></verify>
  <done>Single-provider credentials work; all existing refresh regression tests and new display-isolation assertions pass without changes to polling cadence or data/ledger/chart responsibilities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolver → provider HTTPS | A resolved provider credential authorizes only that provider's existing balance endpoint calls. |
| Display preferences → rendering | Presentation choices must not cross into collection, persistence, ledger, or chart processing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | DataFetcher.refreshAll | high | mitigate | Make resolver input per-provider and omit display preferences from the function/API; regression fixtures compare collection outputs across display states. |
| T-05-07 | Information Disclosure | Provider request path | high | mitigate | Pass resolved key only to its selected existing HTTPS endpoint and retain optional/redacted failure behavior. |
| T-05-SC | Tampering | package installation | low | accept | No npm, pip, cargo, or third-party package installation is planned. |
</threat_model>

<verification>
Run the isolated-scratch focused refresh suite, then run the full suite with the final gate's isolated scratch path after this wave merges with the UI plan.
</verification>

<success_criteria>
SETTINGS-04 is protected by executable evidence that presentation state cannot suppress collection or mutate cache, ledger, chart, OpenAI, or Refresh behavior.
</success_criteria>

<output>
Create `.planning/phases/05-settings-provider-display-preferences/05-03-SUMMARY.md` when done.
</output>
