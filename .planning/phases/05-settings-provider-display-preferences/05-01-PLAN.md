---
phase: 05-settings-provider-display-preferences
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - opencode-widget/Sources/OpencodeWidgetShared/ProviderDisplayPreferences.swift
  - opencode-widget/Tests/OpencodeWidgetSharedTests/ProviderDisplayPreferencesTests.swift
autonomous: true
requirements: [SETTINGS-02, SETTINGS-03]
must_haves:
  truths:
    - "D-02: DeepSeek, MiniMax, and OpenAI use independent typed display preferences and each defaults to visible when the stored value is missing, malformed, or legacy."
    - "D-02: Reapplying a provider's current value changes neither the other two values nor provider data; rapid writes retain the final value for that provider."
    - "D-02: Fixed provider identifiers are independent keys, with no merge or deduplication behavior."
    - "D-02: The visible-card sequence always preserves DeepSeek, MiniMax, OpenAI relative order."
    - "D-02: Disabling all three produces an empty card sequence so the app can render charts-only mode without a balance-card container."
  artifacts:
    - path: opencode-widget/Sources/OpencodeWidgetShared/ProviderDisplayPreferences.swift
      provides: "ProviderID ordering, typed UserDefaults facade, and pure ProviderCardLayout"
      exports: ["ProviderID", "ProviderDisplayPreferences", "ProviderCardLayout"]
    - path: opencode-widget/Tests/OpencodeWidgetSharedTests/ProviderDisplayPreferencesTests.swift
      provides: "Isolated default, persistence, ordering, charts-only, idempotency, and final-write tests"
  key_links:
    - from: ProviderDisplayPreferences
      to: UserDefaults
      via: "object(forKey:) typed Bool recovery and per-provider persistence keys"
      pattern: "object\\(forKey:"
    - from: ProviderCardLayout
      to: ProviderID
      via: "fixed CaseIterable order filtered only by visible state"
      pattern: "ProviderID"
---

<objective>
Build the typed, non-secret presentation contract that makes provider-card selection predictable and independently testable.

Purpose: SETTINGS-02 and SETTINGS-03 require durable display preferences without allowing display state to leak into cache or credentials.
Output: A shared UserDefaults-backed facade and pure ordered-card layout with red-to-green XCTest coverage.
</objective>

<execution_context>
@/Users/aman/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aman/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-settings-provider-display-preferences/05-SPEC.md
@.planning/phases/05-settings-provider-display-preferences/05-PATTERNS.md
<interfaces>
From `Sources/OpencodeWidgetShared/DataStore.swift`: `DataStore` owns encoded `WidgetCache` persistence; this plan must not extend it for preferences or secrets.

From `Sources/OpencodeWidgetApp/OpencodeWidgetApp.swift`: `MenuContent` renders DeepSeek and MiniMax before OpenAI, then chart controls, both charts, and the footer. The later UI plan consumes this plan's fixed provider order.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify provider visibility and compact-layout behavior</name>
  <read_first>opencode-widget/Tests/OpencodeWidgetSharedTests/DataStoreTests.swift; opencode-widget/Sources/OpencodeWidgetShared/DataStore.swift; .planning/phases/05-settings-provider-display-preferences/05-SPEC.md</read_first>
  <files>opencode-widget/Tests/OpencodeWidgetSharedTests/ProviderDisplayPreferencesTests.swift</files>
  <behavior>
    - A new suite, a missing key, a non-Bool stored object, and legacy/missing preferences all resolve DeepSeek, MiniMax, and OpenAI to visible.
    - Each provider persists independently; same-value writes preserve other values; rapid per-provider writes expose the final selection.
    - Every visibility combination returns only enabled IDs in deepseek, minimax, openAI order; no IDs returns an empty layout for charts-only rendering.
  </behavior>
  <action>Create isolated XCTest coverage first, using a uniquely named `UserDefaults` suite removed in teardown. Define the expected public API for `ProviderID`, the typed preferences facade, and the pure layout helper before their production implementation exists. Cover every SETTINGS-02/03 acceptance edge from `05-SPEC.md`, including default-visible recovery, idempotency, final-selection-wins, fixed ordering, and all-hidden layout. Commit the intentional red test contract as `test(05-01): specify provider display preferences`.</action>
  <verify><automated>rm -rf /tmp/opencode-widget-phase05-01 && cd opencode-widget && swift test --scratch-path /tmp/opencode-widget-phase05-01 --filter ProviderDisplayPreferencesTests</automated></verify>
  <done>The new focused test target fails only because the production preference and layout symbols are not implemented; it does not use the app's real defaults suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed display preferences and pure card layout</name>
  <read_first>opencode-widget/Tests/OpencodeWidgetSharedTests/ProviderDisplayPreferencesTests.swift; opencode-widget/Sources/OpencodeWidgetShared/DataStore.swift; .planning/phases/05-settings-provider-display-preferences/05-SPEC.md</read_first>
  <files>opencode-widget/Sources/OpencodeWidgetShared/ProviderDisplayPreferences.swift, opencode-widget/Tests/OpencodeWidgetSharedTests/ProviderDisplayPreferencesTests.swift</files>
  <behavior>
    - Missing and malformed stored values are visible rather than treated as false.
    - Visibility writes are scoped to one ProviderID and are immediately readable from the injected suite.
    - ProviderCardLayout returns the fixed enabled sequence and no cards for charts-only mode.
  </behavior>
  <action>Implement `ProviderID: String, CaseIterable, Sendable` in the fixed `.deepseek`, `.minimax`, `.openAI` order and a typed `ProviderDisplayPreferences` initialized with an injectable `UserDefaults`. Use stable namespaced non-secret keys selected here: `provider-card-visible.deepseek`, `provider-card-visible.minimax`, and `provider-card-visible.openai`; read each through `object(forKey:) as? Bool ?? true`. Add `ProviderCardLayout.visibleCards(preferences:)` as a pure ordered filter. Keep this layer presentation-only: do not import `DataStore`, encode `WidgetCache`, or admit credentials. Make the red tests green and commit `feat(05-01): persist provider display preferences`.</action>
  <verify><automated>rm -rf /tmp/opencode-widget-phase05-01 && cd opencode-widget && swift test --scratch-path /tmp/opencode-widget-phase05-01 --filter ProviderDisplayPreferencesTests</automated></verify>
  <done>All visibility/default/order/charts-only tests pass, and the shared module contains no credential, cache, ledger, chart, or network dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings UI → UserDefaults | Non-secret provider display choices cross into persistent local storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | ProviderDisplayPreferences | medium | mitigate | Typed per-provider reads recover to visible and isolated XCTest proves one write cannot affect sibling preferences. |
| T-05-02 | Information Disclosure | UserDefaults | medium | mitigate | The facade stores only Boolean presentation values and has no credential API. |
| T-05-SC | Tampering | package installation | low | accept | No npm, pip, cargo, or third-party package installation is planned. |
</threat_model>

<verification>
Run the isolated-scratch focused test command after the red and green commits, then retain the suite for downstream UI integration.
</verification>

<success_criteria>
SETTINGS-02 and SETTINGS-03 have deterministic, isolated automated coverage for default recovery, independent persistence, fixed order, and all-hidden layout.
</success_criteria>

<output>
Create `.planning/phases/05-settings-provider-display-preferences/05-01-SUMMARY.md` when done.
</output>
