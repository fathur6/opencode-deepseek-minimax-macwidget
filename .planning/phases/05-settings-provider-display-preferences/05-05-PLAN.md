---
phase: 05-settings-provider-display-preferences
plan: 05
type: execute
wave: 3
depends_on: [05-03, 05-04]
files_modified: []
autonomous: false
requirements: [SETTINGS-01, SETTINGS-02, SETTINGS-03, SETTINGS-04, SETTINGS-05, SETTINGS-06, SETTINGS-07]
user_setup:
  - service: DeepSeek and MiniMax
    why: "Live validate-and-save verification requires Aman-provided provider API keys."
  - service: Codex CLI
    why: "Connected-state verification requires an existing local Codex OAuth session created by Aman running codex login."
must_haves:
  truths:
    - "Repeated Settings actions present one focused native window; rapid activation has the same singleton result."
    - "Missing/malformed/legacy preference state defaults visible, fixed card order remains stable, and all-hidden is charts-only with controls and actions retained."
    - "Display toggles are idempotent/final-selection-wins and leave polling, cache, ledger, Refresh, and chart series unchanged."
    - "Keychain-first per-provider resolution and removal preserve the legacy auth file exactly."
    - "Valid selected-provider setup persists in Keychain; invalid replacement retains the previous connection; final valid concurrent request wins."
    - "Codex missing/malformed status is Not connected and the exact copy-only guidance creates no authentication side effect."
    - "No provider API key or Codex token is rendered, logged, cached, or persisted outside Keychain."
  artifacts:
    - path: opencode-widget/.build
      provides: "Successful SwiftPM test/build products after the phase"
  key_links:
    - from: MenuContent SettingsLink
      to: ProviderSettingsView
      via: "native SwiftUI Settings scene"
      pattern: "SettingsLink"
    - from: ProviderSetupCoordinator
      to: macOS Keychain
      via: "validated per-provider Keychain upsert/removal"
      pattern: "ProviderCredentialStore"
---

<objective>
Run the complete automated suite, then obtain the required native Settings, Keychain, and live-credential verification from Aman.

Purpose: XCTest proves contracts; macOS window focus, actual Keychain persistence, and live provider accounts require a final observed gate.
Output: Full test evidence and an approved or issue-bearing manual verification result.
</objective>

<execution_context>
@/Users/aman/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aman/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-settings-provider-display-preferences/05-SPEC.md
@.planning/phases/05-settings-provider-display-preferences/05-VALIDATION.md
@.planning/phases/05-settings-provider-display-preferences/05-03-SUMMARY.md
@.planning/phases/05-settings-provider-display-preferences/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute phase-wide automated verification</name>
  <read_first>.planning/phases/05-settings-provider-display-preferences/05-VALIDATION.md; .planning/phases/05-settings-provider-display-preferences/05-SPEC.md</read_first>
  <files>None — verification-only task</files>
  <action>Run the focused Provider test suites followed by the full SwiftPM suite. Record any failure with its test name and stop for correction rather than weakening the requirement. Confirm the phase has no new external dependency or package install, and prepare the concrete application build command for the manual gate.</action>
  <verify><automated>rm -rf /tmp/opencode-widget-phase05-final && cd opencode-widget && swift test --scratch-path /tmp/opencode-widget-phase05-final --filter Provider && swift test --scratch-path /tmp/opencode-widget-phase05-final</automated></verify>
  <done>All focused and full automated tests are green before human verification begins.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify native Settings, Keychain persistence, and live credentials</name>
  <read_first>.planning/phases/05-settings-provider-display-preferences/05-SPEC.md; .planning/phases/05-settings-provider-display-preferences/05-VALIDATION.md</read_first>
  <files>None — manual native macOS verification</files>
  <action>After the automated suite and application build are green, ask Aman to perform the listed native settings and credential checks using real local accounts. This gate validates only interactions that XCTest cannot faithfully observe; it must not request secrets in chat or record them in planning artifacts.</action>
  <what-built>Native singleton Providers Settings, typed card visibility, Keychain-backed independent DeepSeek/MiniMax setup, legacy fallback, and status/copy-only Codex guidance.</what-built>
  <how-to-verify>
1. Build and launch the app, open the popover, and confirm the footer is Refresh, Settings, Quit. Activate Settings repeatedly and confirm it opens/fronts one bounded, scrollable Providers window.
2. Toggle each provider independently, close/reopen the popover and relaunch the app, then confirm the final selections persist, card order stays DeepSeek/MiniMax/OpenAI, and hiding all cards leaves both charts, chart navigation, Refresh, Settings, and Quit with no blank card block.
3. Confirm toggling cards does not stop refreshes or alter chart content.
4. Configure DeepSeek and MiniMax one at a time with valid API keys; confirm each validates, survives relaunch, and refreshes. Attempt an invalid replacement and confirm the prior working connection remains. Remove one configured key and verify its legacy fallback, if present, is still usable without changing the legacy file.
5. With and without a usable local Codex session, confirm OpenAI reports Connected or Not connected. Use the copy control and paste into a text editor to confirm it is `codex login`; confirm no Terminal or authentication action launches.
6. Inspect the macOS Keychain entry metadata for the stable service and provider accounts; never copy/display secret values. Confirm no key/token is visible in the Settings UI or application cache/ledger files.
  </how-to-verify>
  <verify><automated>rm -rf /tmp/opencode-widget-phase05-final && cd opencode-widget && swift test --scratch-path /tmp/opencode-widget-phase05-final --filter Provider && swift test --scratch-path /tmp/opencode-widget-phase05-final && xcodegen generate && xcodebuild -scheme OpencodeWidgetApp -configuration Debug build</automated><human-check>Complete all six native verification steps with live credentials kept out of chat and logs.</human-check></verify>
  <resume-signal>Type "approved" or describe the failed step and observed behavior.</resume-signal>
  <done>Aman approves the singleton Settings window, bounded scrolling, live independent Keychain setup/rollback, legacy fallback, and Codex copy-only behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human-provided API credential → app | Live secrets are entered locally into secure fields and must not enter task logs or planning artifacts. |
| App → macOS Keychain | Persistent secrets are inspected only by metadata/behavior, not copied into verification evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Information Disclosure | manual verification | high | mitigate | Instruct Aman not to send or record secrets; inspect Keychain metadata only and confirm behavior through refresh/relaunch. |
| T-05-13 | Elevation of Privilege | Codex guidance | high | mitigate | Human confirms copy-only command behavior and absence of Terminal/auth execution. |
| T-05-SC | Tampering | package installation | low | accept | No npm, pip, cargo, or third-party package installation is planned. |
</threat_model>

<verification>
Automated: use the final task's isolated-scratch focused/full SwiftPM command, then `xcodegen generate && xcodebuild -scheme OpencodeWidgetApp -configuration Debug build` from `opencode-widget`.

Human: the blocking native macOS verification checklist above.
</verification>

<success_criteria>
All seven SETTINGS requirements have focused automated coverage and Aman has approved the real Settings, Keychain, live provider, and Codex scenarios.
</success_criteria>

<output>
Create `.planning/phases/05-settings-provider-display-preferences/05-05-SUMMARY.md` when done.
</output>
