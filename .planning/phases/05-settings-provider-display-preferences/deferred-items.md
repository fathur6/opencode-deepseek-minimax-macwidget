# Deferred Items

- `opencode-widget/Sources/OpencodeWidgetLedger/QuotaLedger.swift:105` emits the pre-existing Swift warning `result of call to 'withCString' is unused` during focused tests. It is outside Plan 05-01's presentation-preferences scope and does not affect the passing test suite.
