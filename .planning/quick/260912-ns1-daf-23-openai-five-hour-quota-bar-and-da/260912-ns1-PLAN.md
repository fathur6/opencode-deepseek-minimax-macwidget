---
phase: quick-260912-ns1
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DAF-23, QUOTA-01, QUOTA-02, QUOTA-03, TIME-01, TIME-02, TIME-03, TIME-04, UIUX-01, UIUX-02, UIUX-03, UIUX-04, UIUX-05]
files_modified:
  - opencode-widget/Sources/OpencodeWidgetShared/Models.swift
  - opencode-widget/Sources/OpencodeWidgetApp/OpenAIQuotaFetcher.swift
  - opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift
  - opencode-widget/Sources/OpencodeWidgetApp/OpencodeWidgetApp.swift
  - opencode-widget/Sources/OpencodeWidgetApp/QuotaResetBar.swift
  - opencode-widget/Sources/OpencodeWidgetApp/QuotaLedgerService.swift
  - opencode-widget/Sources/OpencodeWidgetLedger/QuotaLedger.swift
  - opencode-widget/Sources/OpencodeWidgetLedger/QuotaSnapshotRow.swift
  - opencode-widget/Tests/OpencodeWidgetAppTests/OpenAIQuotaFetcherTests.swift
  - opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift
  - opencode-widget/Tests/OpencodeWidgetAppTests/MenuContentTests.swift
  - opencode-widget/Tests/OpencodeWidgetAppTests/QuotaLedgerServiceTests.swift
  - opencode-widget/Tests/OpencodeWidgetSharedTests/ModelsTests.swift
  - opencode-widget/Tests/OpencodeWidgetSharedTests/DataStoreTests.swift
  - opencode-widget/Tests/OpencodeWidgetLedgerTests/QuotaLedgerTests.swift
  - README.md
  - docs/releases/v1.3.0.md
  - opencode-widget/project.yml
  - opencode-widget/OpencodeWidgetApp/Info.plist
  - opencode-widget/OpencodeWidgetApp.xcodeproj/project.pbxproj
  - opencode-widget/package-dmg.sh
  - .planning/quick/260912-ns1-daf-23-openai-five-hour-quota-bar-and-da/260912-ns1-SUMMARY.md
must_haves:
  truths:
    - The existing monochrome OpenAI card shows labeled 5h quota above labeled Weekly quota, each with its own remaining percentage, usage fill, reset date and elapsed marker; the cost estimate remains weekly-anchored.
    - One existing OpenAI HTTP request resolves both windows by duration, independent of primary/secondary placement; an unavailable window cannot discard its valid sibling.
    - Legacy cache files still load; refresh failures retain last-known readings independently for each window.
    - The existing quota.db gains nullable five-hour quota fields through an additive, repeatable migration without changing any existing row, old field, usage/cost value or month marker.
    - Parent-owned release and deployment verification ties v1.3.0 source, DMG, installed binary, preserved database and Linear evidence together.
  artifacts:
    - path: opencode-widget/Sources/OpencodeWidgetApp/OpenAIQuotaFetcher.swift
      provides: Duration-based, independently tolerant decoding from a single response
    - path: opencode-widget/Sources/OpencodeWidgetShared/Models.swift
      provides: Backward-compatible five-hour fields and duration-aware pure marker math
    - path: opencode-widget/Sources/OpencodeWidgetLedger/QuotaLedger.swift
      provides: Additive five-hour persistence, preserving existing schema and values
    - path: docs/releases/v1.3.0.md
      provides: Accurate release notes and measured Release-build evidence
  key_links:
    - from: OpenAIQuotaFetcher.fetch
      to: DataFetcher.refreshAll
      via: One response containing independently classified five-hour and weekly values
    - from: WidgetCache.openAIQuota
      to: MenuBarState.update and QuotaLedgerService.recordRefresh
      via: Independent cache fallback feeding UI and durable storage
    - from: QuotaLedger.record
      to: QuotaLedger.rows and recentSnapshots
      via: Nullable added columns, bound parameters and non-destructive upserts
    - from: QuotaResetBar
      to: QuotaResetTimeline
      via: Explicit cycle length and existing minute-granularity render clock
---

<objective>
Deliver DAF-23: add the five-hour OpenAI quota above weekly quota while preserving the cost estimate, cache compatibility and all historical database data, then hand the verified changes through the parent's v1.3.0 release/deployment workflow.

This is one user-requested quick plan with three sequential tasks, not a replan of the stale four-phase milestone. No implementation, build, commit, release, deployment or Linear mutation occurs during planning. Task 1 is the code/regression handoff; tasks 2 and 3 are parent-owned. The parent also owns pre-change baseline measurement and consistent backups. Do not let a delegated code executor perform the parent's external side effects.
</objective>

<execution_context>
@/Users/aman/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/aman/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
Repository: /Users/aman/Documents/opencode-plugin. Set every shell workdir explicitly; the harness working directory is unrelated. File paths below are relative to this repository.

@/Users/aman/Documents/opencode-plugin/AGENTS.md
@/Users/aman/Documents/opencode-plugin/.planning/STATE.md
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetShared/Models.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetApp/OpenAIQuotaFetcher.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetApp/OpencodeWidgetApp.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetApp/QuotaResetBar.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetApp/QuotaLedgerService.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetLedger/QuotaLedger.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/Sources/OpencodeWidgetLedger/QuotaSnapshotRow.swift
@/Users/aman/Documents/opencode-plugin/opencode-widget/package-dmg.sh

Linear-first was completed by the parent: project OpenCode macOS Usage Widget (dc590b16-115f-453a-88d5-3e86b01a1e88), DAF-12 Done, DAF-23 active and approved for source/release/deployment/writeback. Verified remote: public fathur6/opencode-deepseek-minimax-macwidget. Starting commit: 996efde. Branch: daf-23-openai-five-hour-quota.

Discovery level 0: existing Foundation/SwiftUI/SQLite/XCTest patterns, no new dependencies. Package.swift uses Swift tools 6.3; verify the installed toolchain rather than weakening the manifest. Graph query returned stale locations; current source is authoritative. The actual shared model path is Sources/OpencodeWidgetShared/Models.swift, not Shared/Models.swift.

User decisions override historical AGENTS/STATE assumptions that secondary always means weekly. Classify limit_window_seconds 18000 and 604800 exclusively. Existing remainingPercent/resetDate and openai_percent retain their weekly meaning. Existing polling is 900 seconds and the bar ticker is 60 seconds. QuotaLedgerService.costWindowStart remains resetDate minus 168 hours.

Safety prerequisite, parent-owned: record working-tree status and baseline Release metrics; stop or otherwise quiesce the old app for a consistent SQLite backup of ~/Library/Application Support/OpencodeWidgetApp/quota.db and cache. Account for WAL if present. Preserve every pre-existing untracked file and directory, including .claude, .opencode, duplicate documents/projects, tool-repair-proxy and all existing .derivedData variants. No git clean, hard reset, broad staging, recursive mirror deletion or copying over unknown files. Do not edit user-owned untracked design documents.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement duration-classified dual quota, durable migration and regressions</name>
  <files>opencode-widget/Sources/OpencodeWidgetShared/Models.swift, opencode-widget/Sources/OpencodeWidgetApp/OpenAIQuotaFetcher.swift, opencode-widget/Sources/OpencodeWidgetApp/DataFetcher.swift, opencode-widget/Sources/OpencodeWidgetApp/OpencodeWidgetApp.swift, opencode-widget/Sources/OpencodeWidgetApp/QuotaResetBar.swift, opencode-widget/Sources/OpencodeWidgetApp/QuotaLedgerService.swift, opencode-widget/Sources/OpencodeWidgetLedger/QuotaLedger.swift, opencode-widget/Sources/OpencodeWidgetLedger/QuotaSnapshotRow.swift, opencode-widget/Tests/OpencodeWidgetAppTests/OpenAIQuotaFetcherTests.swift, opencode-widget/Tests/OpencodeWidgetAppTests/DataFetcherTests.swift, opencode-widget/Tests/OpencodeWidgetAppTests/MenuContentTests.swift, opencode-widget/Tests/OpencodeWidgetAppTests/QuotaLedgerServiceTests.swift, opencode-widget/Tests/OpencodeWidgetSharedTests/ModelsTests.swift, opencode-widget/Tests/OpencodeWidgetSharedTests/DataStoreTests.swift, opencode-widget/Tests/OpencodeWidgetLedgerTests/QuotaLedgerTests.swift</files>
  <behavior>
    - Both orderings of 18000/604800 yield identical five-hour/weekly values; weekly-only primary remains weekly and five-hour-only primary never becomes weekly.
    - Missing/null/unknown duration, malformed window objects, wrong numeric types, absent usage, out-of-range percentages and invalid reset values do not invalidate the valid sibling. Missing/bad reset allows valid percentage with unavailable reset, never an invented date.
    - HTTP/auth/JSON failures retain each cached window; a valid fresh window replaces only its own cached reading. A fresh percentage with no reset must not inherit an unrelated stale reset. Both refreshAll credential branches behave identically, with exactly one quota-fetch invocation.
    - Old cache JSON decodes unchanged with five-hour fields nil; new JSON round-trips both windows, all provider fields, histories and usage/cost data through DataStore.
    - A five-hour cycle at start, halfway, one minute later, reset and after reset gives bounded continuous fractions, including 0, 0.5 and 1; weekly default remains 168 hours, uses absolute server time and stays clamped.
    - Migration of both original and current populated schemas preserves all old column values and rows, including NULLs, zero values, source, recorded_at, usage/cost and quota_month_mark; reopening repeatedly changes nothing. Added values round-trip through both row readers and survive reopen.
    - Same-hour nil updates preserve prior five-hour/weekly/usage/cost values; a five-hour-only refresh is recorded. Weekly chart history and estimated-cost interval remain weekly, unaffected by five-hour resets.
  </behavior>
  <action>
    Work only on task 1's source and regression files after the parent confirms the safety prerequisites. Add failing XCTest cases before changing production behavior, using existing URLProtocol injection, temporary cache directories and temporary SQLite files. Never use production credentials/databases in tests. Do not commit in this delegated task.

    Extend OpenAIQuota with optional fiveHourRemainingPercent and fiveHourResetDate, defaulting to nil in the initializer and absent-key decoding. Retain the existing weekly fields and serialized keys so old caches and call sites remain compatible; no model replacement or unrelated refactor is needed. Extend QuotaSnapshotRow with corresponding optional fields and source-compatible defaulted initializer arguments.

    Add limit_window_seconds decoding in OpenAIQuotaFetcher. Decode primary and secondary independently with loss-tolerant window/field decoding, classifying only validated numeric duration 18000 or 604800. Do not infer identity from position, reset proximity or absence of the other window. Unknown or missing duration is unavailable. Validate finite percent in 0...100 and compute 100 minus used_percent. Accept integer or fractional JSON numbers for reset_at, reject nonfinite/nonpositive or unrepresentable timestamps and expose no reset when invalid. Select the first valid candidate per duration in stable primary-then-secondary order if duplicated; a malformed duplicate cannot override a valid one. Return nil when neither window has valid quota, otherwise a partial OpenAIQuota. Keep the endpoint, OAuth headers, timeout and single-request transport unchanged.

    In DataFetcher.refreshAll merge fresh and cached values per window as a pair, not whole-object fallback: if a window has valid fresh percent use its percent and optional reset; otherwise retain that window's cached pair. Apply one shared merge helper to both the missing-OpenCode-auth and normal branches. The single fetchedOpenAIQuotaTask remains the only quota fetch. Missing first-run data is nil, not zero or full quota.

    Parameterize QuotaResetTimeline with a default 168-hour cycle and explicit 5-hour support; preserve its public weekly default. Compute elapsed fraction from unrounded seconds and clamp 0...1, rounding only display text. Correct existing tests that enshrine whole-hour marker quantization rather than propagating that bug to the five-hour bar. Parameterize QuotaResetBar similarly and retain the existing minute ticker; suppress usage fill when percent is unavailable and suppress the marker when reset is unavailable. Guard finite values and invalid cycle lengths without division by zero. Do not introduce faster ticks or extra polls.

    Within the current clickable OpenAI card, keep its title and Est. cost header; render labeled 5h content above labeled Weekly content, each with its own remaining text, bar and existing local-date/time reset formatting. Use the current monochrome track/fill/fonts/spacing and panel width; unavailable data displays Quota unavailable and an empty track rather than an apparent exhausted quota. Preserve click destination, other cards, charts and Refresh/Quit behavior. Keep MenuBarState estimated cost and the historical OpenAI chart anchored exclusively to weekly fields. Add helper/state assertions for independent labels/readings and unchanged cost anchor; visual layout is checked by the parent.

    Add nullable openai_five_hour_percent REAL and openai_five_hour_reset_at REAL columns to the existing quota_snapshots table; reset is stored as Unix seconds. Use PRAGMA table_info to identify absent columns, add them transactionally, and verify SQLite return codes, rolling back/reporting a sanitized migration failure rather than treating arbitrary SQL errors as successful duplicate-column migration. Apply the same idempotent mechanism to the three existing usage/cost additions so original and current schema fixtures are supported. Never rebuild/drop/replace the table, rename or overwrite old columns, backfill five-hour values from weekly data, or invoke prune as part of migration. Assert migration equivalence before any ordinary retention operation; routine existing retention is a separate policy, not migration data loss.

    Extend record and both rows/recentSnapshots SELECT/mapping paths with bound nullable values. Preserve current COALESCE upserts for all old fields and use equivalent non-destructive semantics for new fields. Wire QuotaLedgerService.recordRefresh to pass five-hour values, include five-hour-only data in its guard and source label, and preserve them through seededCache. Leave cost aggregation, weekly backfill and chart series semantics unchanged. Tests must compare raw old-column tuples including recorded_at and month marks, not just row count, across initial migration and repeated reopen. Include five-hour insert/update/read/reopen and nil-upsert assertions with nonzero usage/cost values.
  </action>
  <verify>
    <automated>swift test --package-path /Users/aman/Documents/opencode-plugin/opencode-widget --filter 'OpenAIQuotaFetcherTests|DataFetcherTests|MenuContentTests|QuotaLedgerServiceTests|ModelsTests|DataStoreTests|QuotaLedgerTests'</automated>
    <human-check>Parent inspects 5h-above-Weekly layout in light/dark appearance, separate unavailable states, unclipped menu actions and preserved Est. cost; checks menu reopen recomputes both markers.</human-check>
  </verify>
  <done>All targeted regressions pass; one request populates both duration-classified windows; old caches and all original database values survive; both bars and weekly cost are wired through the existing refresh path. Provide the parent changed-file list, test results and remaining risks without publishing or deployment.</done>
</task>

<task type="auto">
  <name>Task 2: Parent-owned release documentation, Release build and performance evidence</name>
  <files>README.md, docs/releases/v1.3.0.md, opencode-widget/project.yml, opencode-widget/OpencodeWidgetApp/Info.plist, opencode-widget/OpencodeWidgetApp.xcodeproj/project.pbxproj, opencode-widget/package-dmg.sh</files>
  <action>
    OWNER: parent orchestrator, after task 1. Review the source diff and baseline comparison, run the full suite, then update README and v1.3.0 notes: labeled dual quota, duration classification, independent cached fallback, unchanged estimated weekly cost, additive database compatibility, unchanged polling cadence and actual install/security caveats. Do not claim notarization or fresh data when not verified. Put 1.3.0 release version and a monotonically increasing build number into XcodeGen's authoritative settings/info properties; regenerate the project and verify the resulting app Info.plist rather than merely changing release prose.

    The current package-dmg.sh deletes pre-existing .derivedData-release and build paths. Before invoking packaging, minimally change it to use a fresh mktemp-owned staging/DerivedData directory and an explicit non-colliding output destination. Cleanup must be restricted to the invocation-owned temporary directory; fail rather than overwrite an existing artifact. Preserve its Release build, resource copying, narrowly scoped generated-bundle xattr cleanup, ad-hoc signing and /Applications shortcut. Preserve all pre-existing untracked build directories and artifacts.

    Build/package with the installed compatible Swift/Xcode toolchain. Parent measures baseline and new Release builds on the same machine with the same fixture/live-data conditions and menu states: launch duration, idle/menu-open CPU and RSS, refresh duration and quota HTTP count. Use repeated comparable samples; record method, raw summaries and deltas, identify regressions and explain or fix them before release rather than inventing a pass threshold. Verify the second bar causes no additional HTTP requests and ticks no faster than once per minute. Use isolated database copies for baseline/new comparisons; only controlled deployment may touch live data.

    Verify the DMG and signed generated app, inspect embedded version, and retain SHA-256 plus source revision in parent evidence. Keep local database contents, auth, account identifiers and private machine paths out of public notes. Parent owns any authorized source commit/push after status/diff review and stages only intended files; this planning run performs none.
  </action>
  <verify>
    <automated>swift test --package-path /Users/aman/Documents/opencode-plugin/opencode-widget &amp;&amp; bash -n /Users/aman/Documents/opencode-plugin/opencode-widget/package-dmg.sh &amp;&amp; git -C /Users/aman/Documents/opencode-plugin diff --check</automated>
    <human-check>Parent additionally records successful Release xcodebuild and packaging commands with their actual isolated paths, hdiutil verify, codesign --verify --deep --strict, embedded 1.3.0 version, artifact SHA-256 and measured performance comparison. Build/signing/measurement failures block publication.</human-check>
  </verify>
  <done>Full regression suite passes, accurate release notes exist, an independently verified v1.3.0 Release DMG is ready, and measured performance/request-count evidence is available without deleting user-owned artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Parent-owned publication, cloud-clone synchronization, deployment and Linear writeback</name>
  <files>.planning/quick/260912-ns1-daf-23-openai-five-hour-quota-bar-and-da/260912-ns1-SUMMARY.md</files>
  <action>
    OWNER: parent orchestrator only. Task 2 verification and backups are prerequisites. Use gh against the verified fathur6/opencode-deepseek-minimax-macwidget repository for the already-authorized source/release workflow. Verify the intended commit and target branch before publishing v1.3.0 with the tested DMG and notes. Do not overwrite an existing release/tag or force-push; reconcile any collision explicitly. Download/verify the published asset against the local SHA-256 and inspect its embedded app version.

    Resolve the actual local cloud clone through the parent's known path and its git remote/status, not the harness directory. Sync only reviewed tracked changes using non-destructive Git operations; preserve all existing untracked files and local modifications and stop on conflicts. Verify source revision parity. Do not synchronize live database/cache/auth into the repository or replace user data as part of application installation.

    Deploy the verified Release app to the parent's confirmed installation target, preserving the backup and existing Application Support location. Stop the old process before replacement and verify the relaunched process executable path and bundle version. Compare the migrated live database's old-column projection and month marks to the consistent backup before refresh/upsert/prune can obscure migration equivalence; use the separately migrated backup-copy test when lifecycle writes cannot be paused and document that limitation. After a controlled refresh, query the same existing quota.db to confirm five-hour percent/reset are populated when supplied, weekly/usage/cost remain available, SQLite integrity_check is ok and no unexplained historical rows disappeared. Distinguish normal same-hour updates and retention from migration. Do not mark success if live endpoint availability prevents verifying both bars; report the blocked part and fixture evidence separately.

    Verify labeled 5h above Weekly, independent percentages/reset readouts, marker behavior, unchanged Est. cost, other providers/charts and persistence after restart. Parent writes the summary with actual tests/build/performance, source/release URLs, DMG hash, cloud-clone parity, installation path/version, migration proof and remaining risks. Use authorized Linear MCP writeback to DAF-23 and the project's update, linking evidence and the release; keep DAF-12 unchanged. Mark DAF-23 complete only when its acceptance checks pass and set project health only from actual evidence, not merely local edits. Auth gates or deployment conflicts are returned to the parent without claiming completion.
  </action>
  <verify>
    <automated>gh release view v1.3.0 --repo fathur6/opencode-deepseek-minimax-macwidget --json tagName,targetCommitish,url,assets &amp;&amp; sqlite3 -readonly "/Users/aman/Library/Application Support/OpencodeWidgetApp/quota.db" "PRAGMA integrity_check; SELECT hour, openai_five_hour_percent, openai_five_hour_reset_at, openai_percent, openai_input_tokens, openai_estimated_cost_usd FROM quota_snapshots ORDER BY hour DESC LIMIT 3;"</automated>
    <human-check>Parent verifies published/downloaded hashes, installed process/bundle identity, original-field preservation evidence, cloud-clone parity, dual-bar visual checks and restart persistence. Re-read DAF-23/project update after writeback to confirm the correct records and links.</human-check>
  </verify>
  <done>Verified source and v1.3.0 DMG are published, confirmed cloud clone and installed app match, existing user data is preserved with live five-hour persistence demonstrated, and Linear records contain evidence-backed status and links.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| OAuth/local auth to provider HTTP | Credentials stay in-memory and response JSON is untrusted. |
| Response/cache to UI and SQLite | Malformed/missing windows must not poison sibling readings or historical data. |
| Existing SQLite file to migrated schema | Persistent personal usage/cost and month marks cannot be recreated or lost. |
| Working tree to public release/local install | Untracked user files and private data must not enter release or be overwritten. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-DAF23-01 | Tampering | OpenAIQuotaFetcher | medium | mitigate | Task 1 validates duration, percent and reset independently with malformed/reversed fixtures. |
| T-DAF23-02 | Information disclosure | Auth/logs/release notes | high | mitigate | Retain existing OAuth headers; synthetic test credentials only; no auth payloads or private database artifacts in logs/publication. |
| T-DAF23-03 | Tampering | QuotaLedger migration | high | mitigate | Task 1 transactional additive schema, bound writes, raw old-field preservation and repeat-open tests; parent consistent backups. |
| T-DAF23-04 | Denial of service | Refresh/TimelineView | medium | mitigate | One HTTP call per refresh and 60-second render tick; task 2 measures Release CPU/RSS/refresh deltas. |
| T-DAF23-05 | Tampering | Packaging/deployment | high | mitigate | Fresh owned build staging, verified hashes/version/revision, no deletion of pre-existing untracked files, backup-preserving deployment. |
| T-DAF23-06 | Repudiation | Release/Linear status | medium | mitigate | Parent summary and writeback link exact tested source, release artifact and deployment checks; no unsupported success claims. |

No npm/pip/cargo or other new dependency installation is planned; no package-legitimacy checkpoint is required.
</threat_model>

<source_audit>
| Source | ID | Scope/constraint | Coverage | Status |
|--------|----|------------------|----------|--------|
| GOAL | DAF-23 | Labeled 5h above weekly with cost intact and durable storage | Task 1 | COVERED |
| GOAL | ROADMAP | Existing live quota/reset timeline and resilience, not historical branch porting | Task 1 | COVERED for quick-task impact; old phase workflow excluded |
| REQ | QUOTA-01/02/03 | Server reset, dual windows, OAuth/cached fallback | Task 1 | COVERED; duration identity supersedes historical slot assumptions |
| REQ | TIME-01/02/03/04 | Remaining percent, pure server-anchored bounded marker | Task 1 | COVERED; no five-hour whole-hour quantization |
| REQ | UIUX-01/02/03/04/05 | Card/bar placement, monochrome, minute tick, reset time, stale resilience | Tasks 1, 3 | COVERED |
| RESEARCH | Quick task | No task-specific RESEARCH.md supplied; direct current-code discovery, no new dependencies | Task 1 and context | COVERED |
| RESEARCH | AGENTS stack constraints | Foundation/SwiftUI/SQLite, OAuth isolation, no scraping/helpers/extra polling | Tasks 1, 2 | COVERED; slot-based guidance superseded by user |
| CONTEXT | User prompt | 18000/604800 regardless of slot; missing/error isolation; cache compatibility; no extra HTTP | Task 1 | COVERED |
| CONTEXT | User prompt | Additive zero-loss quota.db migration including all existing usage/cost, idempotence and marker regressions | Tasks 1, 3 | COVERED |
| CONTEXT | User prompt | Parent baseline/backups, Release performance, v1.3.0 DMG, cloud clone, deployment, Linear | Prerequisite and tasks 2, 3 | COVERED |
| CONTEXT | User prompt | Single three-task plan; no planner implementation/commits; preserve untracked files | Entire plan | COVERED |

No quick-task CONTEXT.md or D-NN decision list was supplied. Decisions are the explicit user prompt; no invented decision identifiers. Deferred countdowns, notifications, new chart series, variable observed-cycle inference, WidgetKit and unrelated application refactors are excluded.
</source_audit>

<verification>
Task dependency chain: safety baseline/backups (parent) -> task 1 code/regressions -> task 2 Release/build/performance (parent) -> task 3 publication/deployment/writeback (parent). No same-file parallel work. Targeted tests are the fast feedback loop after the initial build; full compilation, Release profiling and deployment are longer parent gates. Use actual test output, not absence of command errors alone. Source changes remain narrow to dual-window plumbing despite spanning existing shared/app/ledger targets.
</verification>

<success_criteria>
- Duration-classified 5h and Weekly readings, separate markers/resets and preserved weekly estimated cost are visible in the existing monochrome card.
- Cache, request-count, reversed/missing/invalid response, five-hour math and raw-data migration/idempotence regressions pass.
- Existing quota.db retains old rows and every old field, and new five-hour observations persist across reopen/restart.
- Parent verifies Release performance, publication, clone synchronization, installed artifact identity and evidence-backed Linear writeback; failures remain explicitly blocked.
- No pre-existing untracked user files are deleted, overwritten or accidentally staged.
</success_criteria>

<output>
Parent creates .planning/quick/260912-ns1-daf-23-openai-five-hour-quota-bar-and-da/260912-ns1-SUMMARY.md after execution, distinguishing completed checks from blocked parent-owned actions. Planning modifies only this PLAN.md and creates no commits.
</output>
